# THT＋BX 組合回測（TSLA 日K）

獨立子目錄，不改動 repo 其他程式。評估 THT＋BX（可選 RSI2）進出場組合，並用建議組合回測約五年。

完整說明見 `report.md`（執行 `python run_backtest.py` 後產生）。

```bash
pip install -r requirements.txt
python run_backtest.py
python test_indicators.py
```
